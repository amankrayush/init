{
    "Comment": "Creating Alias resource record set in Route 53",
    "Changes": [
      {
        "Action": "UPSERT",
        "ResourceRecordSet": {
          "Name": "speechassistant.mybahmni.in.",
          "Type": "A",
          "TTL": 300,
          "ResourceRecords": [{"Value": ${{ steps.get-publicIpAddress.outputs.publicIpAddress }}}]
        }
      }
    ]
  }
